<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Development only: the FastAPI server runs on the user's local machine. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
