---
description: H5 演示端代码约定（Vite + React，离线快照优先）
globs: "h5-demo/**/*.{js,jsx,ts,tsx}"
---

# H5 演示端约定

## 结构

```
h5-demo/
  index.html
  vite.config.js
  src/
    main.jsx              # 入口
    App.jsx               # 主界面
    api.js                # 后端 API 客户端
    scenarios.js          # 演示场景
    offline-snapshots.json# 离线快照（无后端也能演示）
    chatSignals.js        # 交互信号
    themes.js             # 主题
    styles.css
  dist/                   # 构建产物
```

## 硬性要求

1. **离线快照优先。** `offline-snapshots.json` 保证不连后端也能演示完整流程。
   **不要**让默认路径强依赖后端可用。
2. **演示必须贴合真实规则输出。** 快照里的建议必须来自规则引擎的真实产出，
   **不允许**手工编一条「看起来漂亮」的推荐。
3. **证据要能看见。** 每条装备建议要能展开官方装备文本证据与规则版本。
4. **约束要展示。** 金币 / 格子 / 唯一组 / 次数（复活甲限次、冷却）这些约束的满足情况
   应在界面上可见，这是规则引擎可解释性的体现。
5. **数字与后端一致。** 132 英雄 / 121 装备 / 26 类装备标签 / 9 类英雄威胁标签，
   前端展示的数字必须与 `docs/GAME_BREAKDOWN.md` 一致 **且不得暗示 121 件全部带标签**。

## 改完必跑

```bash
cd h5-demo
npm run dev        # 本地预览
npm run build      # 确认可构建（产物 dist/）
```

## 常见坑

- **`api.js` 与后端契约手工同步**，改后端接口要同时改它。
- **`offline-snapshots.json` 是手写产物** —— 更新时要重新从真实规则输出生成，
  不要手改数字。
- **构建产物 `dist/` 会被 GitHub Pages 部署**（见 `.github/workflows/deploy-h5-demo.yml`），
  改完记得重新 build 并提交。
- **不要新增付费 API 调用** —— 本项目零成本本地运行。

## 不要做

- ❌ 不要为了演示效果手工编造推荐结果。
- ❌ 不要隐藏约束不满足的情况。
- ❌ 不要暗示 121 件装备全部带人工标签（真值是 48 件）。
